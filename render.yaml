services:
  - type: web
    name: aevorin-api
    runtime: node
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      - key: SUPABASE_DB_URL
        sync: false
